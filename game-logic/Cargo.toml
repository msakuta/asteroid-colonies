[package]
name = "asteroid-colonies-logic"
version = "0.1.0"
edition = "2021"
description = "Common logic between the server and the frontend"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.8.5"
bincode = "1.3.3"
